# Copy this file to ui/.env and fill in your values.
# ui/.env is gitignored — never commit real values.
# Variables must be prefixed with REACT_APP_ to be available in the browser.

# Support email shown in error messages and the help guide
REACT_APP_SUPPORT_EMAIL=your-support-email@example.com
